kind: Deployment
apiVersion: apps/v1
metadata:
  name: protools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: protools
  template:
    metadata:
      labels:
        app: protools
    spec:
      containers:
        - name: maquette-protools
          image: mailinenguyen/maquette-protools:latest
          imagePullPolicy: Always
